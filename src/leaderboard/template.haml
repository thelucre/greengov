.leaderboard
	%h1.title The full leaderboard
	%ul.organizations
		%li.header
			%span.rank Rank
			%span.name Agency Name
			%span.pass-fail Pass/Fail
			%span.reduction Reduction
		%li(v-repeat='org in organizationsWithStats')
			%org-detail(org="{{ org }}")
